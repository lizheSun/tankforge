# 坦克大战 · AI 策略对战平台 — 需求文档

> 项目代号：**TANKFORGE（暂定）**
> 版本：v0.1（草案）
> 日期：2026-09-07
> 状态：待评审
> 关联规格：**[战斗系统技术规格 BATTLE_SPEC](./docs/BATTLE_SPEC.md)**（§5 地图/物理/胜负、§6.2 能力点数值表、FR3 决策契约的可开发级定义，以其为准）

> **📌 实现状态说明（开源读者必读）**
> 本文档是项目早期的**需求与设计草案**，完整保留了设计初衷（含部分最终未采用的方案）。
> 截至首个公开版本，主要落地情况与差异如下：
>
> | 本文档的设想 | 最终实现 |
> |---|---|
> | 模型策略用 ONNX Runtime Web 推理（§M5、§1.2） | 改为**自研 MLP 编译方案**：模型存为 tankforge-model JSON（有版本号、带维度校验），编译为确定性 JS 代码在沙箱执行（无需 ONNX） |
> | 策略目录 `brain/` / `model/` | 实际为 `src/sandbox/`（沙箱执行）+ `src/game/modelBrain.ts`（观测/动作/编译） |
> | 训练工具为"可选离线工具" | 实际提供了完整的 `scripts/rl/` 训练管线（进化 / PPO / 自我对弈），且**训练环境桥接真实引擎** |
> | V1 只做 1v1 | 已落地 NvN 团战（数据链 + 通信槽位）、赛事系统（天梯/吃鸡/组队/实时排行榜，MySQL 持久化） |
>
> 一切以 **代码 + [BATTLE_SPEC](./docs/BATTLE_SPEC.md) + [README](./README.md)** 为最终事实来源；本文档仅作设计脉络存档。

---

## 0. 一句话概述

一个运行在**浏览器中的坦克大战竞技平台**，核心玩法是 **AI vs AI（机器对机器）**：用户上传"坦克策略包"，由平台托管的坦克**全自动自主作战**，支持 1v1 单挑、NvN 团战；策略包可定制坦克的属性参数（血量/移速/攻速/伤害等）与决策策略（规则脚本或 AI 模型）。

---

## 1. 背景与目标

### 1.1 背景

传统坦克大战是"人机对战"，参与者是玩家。本项目的差异化定位是 **"人写策略，坦克自己打"** —— 玩家从"操作手"转变为"战术编程员/训练师"：

- 平台不是让玩家握着键盘打坦克，而是让 **两辆（或多辆）由代码/模型驱动的坦克** 在沙盘上自动交战；
- 战斗过程完全可观战（实时 + 回放），具备竞技与观赏价值；
- 策略可以写得非常简单（规则 if-else），也可以非常复杂（行为树 / 强化学习模型），形成从新手到专业研究者的梯度。

### 1.2 产品目标

| 编号 | 目标 | 衡量方式 |
|---|---|---|
| G1 | 用户能在 10 分钟内上传一个"能上场打架"的策略包 | 新用户首战耗时 |
| G2 | 支持机器 vs 机器自动对战，无需人工操作 | 单局全程自动进行 |
| G3 | 支持 1v1 与组队团战 | 功能可用 |
| G4 | 策略差异化明显：属性 build + 决策策略两个维度均可定制 | 对局多样性 |
| G5 | 观战/回放体验酷炫（高科技感 UI） | 用户留存与传播 |

### 1.3 非目标（V1 不做的）

- 不做玩家实时手操模式（仅做"接管/调试"开关）；
- 不做本地下载客户端（纯浏览器，除可选的离线训练工具外）；
- V1 不支持编写 C++/Rust 等编译语言策略（见 6.4 沙箱说明，后续可扩展 WASM）。

---

## 2. 术语表

| 术语 | 含义 |
|---|---|
| 坦克 / Tank | 战场上的对战单位，由策略包驱动 |
| 策略包 / Strategy Pack | 用户上传的最小可对战单元：`manifest + 属性配置 + 决策策略(+可选模型文件)` |
| 属性配置 / Build | 物理参数：血量、移速、攻速、伤害、转向、视野等（受"能力点预算"约束） |
| 决策策略 / Brain | 每帧/每事件决定坦克行为的逻辑：规则代码、行为树，或推理模型 |
| AI 驾驶员 | 引擎为每辆坦克实例化的运行时（沙箱中运行其 Brain） |
| 对局 / Match | 一局完整战斗（两阵营） |
| 帧 / Tick | 引擎推进战斗的时间步，全平台统一频率 |
| 事件 / Event | 战斗期间推送给驾驶员的信息（被击中、雷达发现目标、碰撞等） |
| 意图 / Action | 驾驶员输出的动作请求（move/fire/rotate…），由引擎校验后执行 |
| 回放 / Replay | 基于确定性种子记录的对局录像，可自由拖动视角回放 |
| 能力点 / CP (Capability Points) | 属性 build 的分配预算单位，保证不同坦克同量级对抗 |

---

## 3. 行业对标与设计参考

| 平台 | 形态 | 本平台借鉴点 |
|---|---|---|
| [Robocode Tank Royale](https://robocode.dev/) | 事件驱动 bot API（Java/Python/C#），WebSocket 实时对战，独立进程 bot，可无头批量比赛 | ① 事件驱动决策模型；②"意图→引擎执行"的权威校验；③ 观战器与回放 |
| [CyberTank](https://cybertank.squidcode.com/) | 浏览器 DSL，传感器(detect/ping)、护盾/燃料/弹药资源、3v3 编队 | ① 传感器与有限资源（视野/弹药）造成的不对称博弈；② 浏览器内 DSL 即时对战；③ 3v3 编队槽位模式 |
| [AgenTank](https://agentank.ai/) | Agent-first，JS 策略 `onIdle(me,enemy,game)` 回调，技能/冷却，段位制 | ① 单一决策回调契约；② 段位/天梯驱动竞技生态；③ 公开示例坦克作为上手模板 |
| 百度之星《坦克大战》 | 竞赛形式 AI（启发式搜索对抗） | 对局公平性规则（超时判负、指令合法性）与赛制设计 |

**核心取舍决策（写进架构约束）：**

1. **平台采用"Tick 制确定性引擎 + 意图校验执行"**：与 Robocode/CyberTank 一致，Bot 永远不能直接改坐标/扣血，只能发意图，引擎裁决 → 天然防作弊。
2. **策略语言 V1 = JavaScript（严格沙箱）**：AgenTank 证明了 JS 策略在浏览器端的可行性；同时提供受限 DSL 帮助新手上手（对标 CyberTank）。
3. **属性即 build，受能力点约束**：避免"满配血量+满配伤害"的无脑怪，保证对局是策略博弈而非数值碾压（详见 6.2）。
4. **模型策略与规则策略平权**：模型统一封装为"每帧输入状态向量 → 输出动作概率/离散动作"，由平台推理运行时执行（浏览器端 ONNX Runtime Web），使"强化学习训练师"可参与。

---

## 4. 用户角色与核心使用流程

### 4.1 角色

| 角色 | 描述 |
|---|---|
| 普通玩家 | 上传/选用策略包，发起或参与对战，观战 |
| 策略作者 | 编写规则策略或训练模型，构建属性 build，参加天梯 |
| 房主（自定义房） | 创建房间，设定地图/规则/阵营配置，邀请用户 A/B 上策略 |
| 团队队长 | 组队（车队），编排团战阵容 |
| 管理员 | 平台治理：策略审核、封禁、热更规则 |

### 4.2 核心使用流程（用户 A vs 用户 B）

```
注册/登录
   └─> 新建策略包：命名 + 选底盘模型(属性点分配) + 写 Brain / 传模型
         └─> 沙箱内 本地试跑（Vs 内置 Bot）通过 ✅
              └─> 发布策略包
                   ├─ 发起对战（1v1 匹配 / 自定义房间 / 团战报名）
                   │    · 用户 A 提交策略包 A，用户 B 提交策略包 B
                   │    └─> 引擎加载 → 预检 → 正式对局 → 结算
                   │         · 一方被击穿血量归零 → 另一方胜利
                   │         · 团战：一方全部阵亡 → 另一方胜利
                   └─> 实时观战 / 回放复盘 → 迭代策略包 v2 → 再战（RL 循环）
```

### 4.3 关键场景

1. **单挑（1v1）**：A、B 各带一个策略包，同图对抗，HP 先归零者败（用户原始诉求）。
2. **团战（默认 3v3，可配置 2v2 ~ 5v5）**：两队各由多辆坦克组队，全灭对方即胜；同队协作靠"队友通信"能力（见 6.2 能力项）。
3. **擂台 / 天梯**：公开排行，策略自动接战刷新分数。

---

## 5. 游戏规则总览（战斗基础规则）

> 以下为规则草案，数值可在 P0 阶段调优；**规则一经发布进入赛季即冻结**，保障公平。

### 5.1 战场地图

- **形态**：2D 俯视网格战场，尺寸基准 1200×800（内部以 tick/格统一逻辑），存在若干固定掩体（墙体）与出生点。
- 掩体：实心墙（阻挡坦克与子弹）、可破坏墙（受击积累破坏值）、草丛（遮蔽视野）。
- 出生点对称分布（1v1 对角线；团战按阵营分侧随机候选位）。
- 地图支持多种（开阔/巷战/迷宫），随房主或匹配规则选择。

### 5.2 坦克基础属性基线（供平衡调参）

> 用户在**范围约束 + 能力点预算**内配置这些项（详见 6.2）。以下为"默认 0 加点"基线：

| 属性 | 基线值 | 说明 |
|---|---|---|
| 血量 HP | 100 | 归零即摧毁 |
| 移动速度 | 120 px/s | 前进/倒车 |
| 车体转向 | 90 °/s | |
| 炮塔转速 | 150 °/s | 决定瞄准敏捷度 |
| 攻击间隔 | 0.8 s/发 | 装填周期 |
| 基础伤害 | 12 /发 | 命中结算基准 |
| 弹速 | 480 px/s | 影响预判难度 |
| 视野半径 | 320 px | 探测范围 |
| 弹药/能量 | 无限（V1）/ 可选有限模式 | 有限模式见"竞技模式规则" |
| 命中判定 | 弹丸半径 + 车体矩形碰撞 | 穿透衰减可选 |

### 5.3 战斗推进模型

- **确定性 Tick 引擎**：固定步长（默认 60 tick/s 物理，决策节流 10~20 次/s，降低沙箱负载）。
- **一个 Tick 内流程**：①采集状态 → ②把事件/状态派发给双方驾驶员 → ③收集意图 → ④引擎裁决执行（移动/转向/开火/技能）→ ⑤子弹飞行与命中结算 → ⑥胜负检测。
- 全平台使用**同一随机种子**，保证回放可复现、判定可审计。
- 双方驾驶员在同一战斗进程内并行执行、互不可达（无全局变量共享），杜绝 A 策略直接读取 B 内部状态。

### 5.4 胜负判定

| 模式 | 胜 | 负 | 平 |
|---|---|---|---|
| 1v1 | 对方 HP 归零（被击毁） | 己方被击毁 | 限时（默认 180 s）结束按**剩余 HP 比例**判定，仍平则按造成总伤害 |
| 团战（NvN） | 对方**全部**坦克被击毁 | 己方全灭 | 限时结束按**存活数 > 总剩余 HP > 总伤害**排序，两队并列则判平 |

- 满足用户原始诉求："一方被击中 N 次无血了，那么另一方就胜利了" 即由血量模型覆盖——单发伤害与 HP 在属性配置下可推导出"几发致死"（如 12 伤害 × 9 发 ≈ 100 HP）。
- **掉线/超时/异常策略判负**：驾驶员单次决策超时（默认 100 ms）→ 本 tick 放弃行动；连续 N tick 无响应 → 判"失联"，作为可被攻击的固定靶，最终按上述胜负规则结算；策略代码崩溃 → 当场判负并扣信任分。
- 运行时可配置"打靶时间"等自检规则。

---

## 6. 功能需求（FR）

### FR1 账号与资源管理
- 邮箱/三方登录；个人主页展示：胜率、天梯分、上传策略包列表、战绩、赛季徽章。
- 每个策略包有独立命名空间与版本历史；支持 `v1.2.3` 语义化版本、启用/停用、删除（已在对局中的版本不可删）。

### FR2 策略包（核心实体）— 内容模型

**策略包 = `manifest.json` + 属性配置(加点) + 决策策略(Brain) + 可选模型文件/资产**。

```
my-strategy-pack/
├─ manifest.json        # 名称/版本/作者/底盘型号/能力点分配/策略类型(rule|model|hybrid)
├─ tank.build           # 属性 build：CP 分配结果（JSON，编辑器生成）
├─ brain/               # 规则策略（JS / 受限 DSL）
│   └─ main.js          # 决策入口（契约见 FR3）
└─ model/               # 模型策略（可选，与 brain/ 二选一或 hybrid）
    ├─ policy.onnx      # 训练好的策略网络
    └─ config.json      # 输入特征定义 / 动作空间映射 / 版本
```

**上传与发布流程**：
1. 上传压缩包（≤ 2 MB，解压后 ≤ 20 MB）→ 2. 静态校验（结构/契约/属性点合法性）→ 3. **沙箱试跑**（对内置 Bot 跑一局冒烟测试，输出覆盖/崩溃报告）→ 4. 发布成功 → 5. 可在任意对局中被选用。任一环节失败给出**可读错误报告**（指向代码行）。

### FR3 决策策略契约（SDK 草案）

> 策略作者看到的世界是 **`状态 + 事件 → 意图`**，永远无法直接操纵物理世界。

- **入口 A（推荐 · tick 决策）**：`brain.decide(ctx) -> Action[]`
  - `ctx`：快照 { 自身传感器读数、敌人/队友可观测信息、上次事件、时间、战场地图静态数据、记忆区 }
  - `Action`：`MOVE(forward/back, speed?) / ROTATE(deg) / TURRET_AIM(deg|auto|predict) / FIRE(power?) / SHIELD_ON / SHIELD_OFF / COMMUNICATE(teamId, payload) / NOOP`
- **入口 B（事件驱动）**：可选回调 `onHit(evt)`、`onScan(evt)`、`onCollide(evt)`、`onWallHit(evt)`、`onTick(evt)`（Robocode 风格，给习惯者）。
- **传感器（读）**：雷达/视野内目标（相对方位角/距离/速度估计）、血量、弹药、朝向、炮塔角、冷却剩余、可破坏墙耐久、上一发命中反馈。
- **记忆区**：允许跨帧读写的小型键值存储（上限 4 KB），模拟"战术记忆"，回放中可显示其变化。
- **自检接口**：`selfTest(ctx)` 让策略在提交前声明"我在什么状态下会怎么行动"（用于冒烟测试）。

**瞄准策略**作为 Brain 可调用的"目标锁定意图"提供三种**质量档**（消耗不同，防"默认全员自瞄满级"）：
| 档位 | 实现 | 代价（在能力点中扣除） |
|---|---|---|
| manual（手瞄） | 完全由策略代码计算，需自行预测提前量 | 0 |
| assist（辅助） | 提供敌方相对方位与角速度，作者实现简单 PID | 1 CP |
| auto（自动锁定） | 引擎自动转向指向目标 + 弹道提前量解算，作者只需决定"何时开枪" | 3 CP |

### FR4 坦克属性 Build — 能力点（CP）体系

- 每辆坦克拥有固定预算 **CP（默认 12 点）**，按"底盘模板 + 模块"消费；防全属性拉满的"数值怪"。
- 可加点项（可在范围与步长内连续或分档）：
  - 装甲：HP 上限（±50，成本随档递增）
  - 动力：移动速度、车体转向
  - 火控：攻击间隔（装填）、基础伤害、炮塔转速、弹速
  - 传感：视野半径、瞄准档位（见上表）、扫描精度
  - 战术（团战向）：队友通信带宽 / 指令广播次数
- 编辑器为每个加点给出**可视化对比**（输出该 build 的 DPS/有效血量/机动性三角形雷达图）。
- **公平模式（天梯默认）**：双方 CP 上限一致；**自由模式（自定义房）**：可关闭 CP 上限，允许"满配碾压"实验，房间属性里明确标注。

### FR5 沙箱执行与安全
- 决策代码运行于**强隔离沙箱**：默认 JS。V1 采用"受限 iframe(同源策略)+postMessage"或 **Web Worker + QuickJS-WASM** 沙箱；时间/内存上限：单 tick 决策 ≤ 100 ms，堆内存 ≤ 16 MB，违者熔断并给出栈信息。
- **禁止**：`eval`、`Function`、`fetch`、`XMLHttpRequest`、`import` 外部资源、`Date/random` 滥用（确定性要求，必须走引擎提供的 PRNG）。
- API 面最小化：只暴露契约内传感器/动作/记忆区；不暴露引擎内部、对方内存、浏览器 API。
- 提供静态检查 + 沙箱试跑双保险；试跑超限自动标记"unsafe"禁止上架。
- **策略互相不可见**：双方 bot 各自独立 Worker 进程，仅通过"战场观测"交互。
- 模型推理（onnxruntime-web）：限制输入张量规模与单次推理时长，超出判超时。

### FR6 对局模式

| 模式 | 说明 | 是否天梯计分 |
|---|---|---|
| 快速 1v1 | 系统匹配，上传或选用已发布策略包 | 是 |
| 好友约战 / 房间 | 房主建房：地图、规则、CP、时限、观众权限，邀请 A/B 提交策略 | 否（可开积分） |
| 组队团战 | 每队 1~5 辆坦克：支持(A) 一人编排多策略，或 (B) 多人协作共编一队 | 是（团体分） |
| 锦标赛 | 淘汰赛/循环赛插件化（赛季活动） | 是 |
| 本地试练 | 对内置 Bot 家族 / 历史 Top 回放录制（无积分） | 否 |

- **团战组队流程**：创建车队 → 生成邀请链接 → 队员把自己某策略包槽位"挂载"进队 → 队长锁定阵容提交匹配。

### FR7 实时观战与回放
- 实时观战：延时低至可接受（≤ 500 ms），提供多视角：自由视角、跟随坦克、战术俯视。
- **回放系统**：录帧(事件 + 状态 + 决策轨迹)，可拖动时间轴、倍速 0.5×/1×/2×/4×；叠加 Debug 层（视野圈、索敌射线、意图、路径、命中判定框），用于复盘策略缺陷。
- **决策轨迹可视化**：把 bot 的意图/记忆区状态/事件流同步渲染到 HUD（谁在何时看到谁、为何开火），这是本平台的特色调试体验。

### FR8 匹配与天阶
- 天梯采用 **ELO/Glicko**；团战按"队伍平均分"匹配。
- 赛季制（默认 28 天）：段位（青铜→钛金）、赛季奖励、公开排行榜。
- 反刷分：同 IP/好友高频约战不计天梯分；可疑对局标记人工复核。

### FR9 平台治理
- 策略包上报/审核：违规内容（代码注入、辱骂昵称、泄露他人信息）处理流程。
- 资源配额：每账号策略包数量上限（默认 50）、天梯每日接战次数上限。
- 审计日志：上传、发布、对局关键事件。

---

## 7. 非功能需求（NFR）

| 类别 | 需求 |
|---|---|
| 性能 | 战斗引擎核心更新 ≤ 16 ms/tick；同屏 10 辆坦克 + 弹幕稳定 60 FPS（中端笔记本）；沙箱决策总耗时不影响主线程流畅度 |
| 确定性 | 同一策略包 + 同一种子 → 逐 tick 完全一致（回放与判定的基石） |
| 可用性 | 首包上传到首战 ≤ 10 分钟；错误提示可操作（给出具体行号与修复建议） |
| 安全 | 沙箱逃逸风险 0 容忍；上传文件病毒扫描；对局数据隐私 |
| 兼容 | Chrome/Edge/Firefox/Safari 最新两个大版本；分辨率自适应 1280×720 ~ 4K |
| 可观测 | 服务端可观测（战斗引擎遥测、沙箱崩溃率、匹配队列深度）；关键指标监控告警 |
| 容量 | 支持并行 5000 局/天，回放存储 30 天滚动 |

---

## 8. 技术架构建议（概要）

```
┌────────────────────────────── 浏览器端 ──────────────────────────────┐
│  React/Vue 壳               高性能渲染层 (Canvas2D / WebGL / PixiJS)  │
│   ┌────────────┐  ┌─────────────┐  ┌───────────┐  ┌────────────────┐ │
│   │ 大厅/匹配UI │  │ 策略包编辑器  │  │ 观战/回放  │  │ 沙箱宿主(Worker)│ │
│   └─────┬──────┘  └──────┬──────┘  └─────┬─────┘  └───────┬────────┘ │
│         └────────────────┴──────────────┴─────────────────┘          │
│                           渲染器(Replay 驱动)  ◄── WebSocket/HTTP      │
└──────────────────────────────────┬────────────────────────────────────┘
                                   │ 命令意图 / 状态帧 / 回放流
┌──────────────────────────────────▼────────────────────────────────────┐
│                      对战服务（权威）                                  │
│   房间管理 → 策略加载器 → 确定性引擎(Tick) → 结算服务 → 回放存储       │
│   沙箱执行池(QuickJS-WASM / 受限Worker)                                │
└───────┬───────────────────────┬───────────────────────┬──────────────┘
        │                       │                       │
   PostgreSQL(用户/策略/战绩)  对象存储(回放/模型文件)   队列(匹配/异步)
```

关键决策：
- **权威服务端结算**：即使战术细节在浏览器跑（Room-Local 模式），结果仍可哈希上链/落库防篡改；大规模匹配则全服务端执行。
- **确定性引擎单测**：对物理/弹道/命中写 golden 测试，防止改动破坏回放。
- 模型推理首推 **ONNX Runtime Web**（后端 CPU/WASM，未来 WebGPU），训练端给出 Python 环境模板（离线），使模型与规则策略共享同一对局可执行产物（state→action 标准化）。
- 回放格式：事件 + 每 tick 紧凑二进制（protobuf），浏览器解码回放重演，避免存储完整状态。

---

## 9. UI / UX — 高科技（科幻军工）风格规范

> 视觉总目标：**"全息战术指挥舱 + 军用工控终端"** 质感。参考风格：Robocode 的简洁、HUD 科幻电影(全息战场)、Frostpunk/Cyberpunk 军工 UI。

### 9.1 视觉语言

| 维度 | 规范方向 |
|---|---|
| 基底 | 深空黑蓝 `#06080F ~ #0A1226`，暗部有微弱噪声/网格底纹 |
| 主色 | 荧光青 `#00E5FF`（阵营蓝/己方）、警示橙红 `#FF3B30`（阵营红/敌方血条） |
| 强调色 | 电光紫 `#8A2BE2`、琥珀 `#FFB020`（稀有/金币/赛季） |
| 发光 | 核心描边/血条/瞄准线用 `text-shadow` 式霓虹辉光（box-shadow/glow），敌我能量色编码 |
| 字体 | 数字/标题用未来感等宽字体（如 Orbitron / Rajdhani / Chakra Petch，回退 monospace）；正文用系统无衬线 |
| 质感 | 扫描线叠加（低透明度）、HUD 线段/角标框线(非圆角直角或小圆角)、故障/噪点仅在开战切场时少量使用 |
| 动效 | 250ms 缓动；命中震动、开火后坐、爆炸粒子、弹道曳光；结算数字滚动 |

### 9.2 页面/视图清单

| 视图 | 核心 UI 元素 |
|---|---|
| 启动/登录 | 全屏星空战场动效背景、霓虹 logo、"接入战网"按钮 |
| 主大厅 | 全息雷达中心 + 环形菜单（出战/编队/军械库/排行榜/赛季）；背景飘过战场缩略图 |
| 策略包工作台 | 左：策略包树 + 版本；中：分页签（属性 Build 雷达图加点 / Brain 代码编辑器 / 模型上传 / 试练台）；右：诊断面板（沙箱报错、性能占用、胜率趋势） |
| 属性 Build | 六边形能力图 + 滑块加点，实时预览 DPS/机动/生存力 |
| 匹配/房间 | 两侧"选手卡"展示双方坦克(属性数值+策略包名+胜率)，中央地图预览 + 倒计时 |
| 对局观战 | 战场主视口 + 顶部事件流、左侧阵营状态栏、底部时间轴(拖动回放)，可切 Debug 层 |
| 结算 | 胜/败大标题 + 双方剩余血量/伤害统计 + 关键转折事件时间线 + "再来一局/复盘/修改策略" |
| 团战大厅 | 5×2 槽位编队面板，成员策略卡拖入挂载，队长锁定阵容 |

### 9.3 HUD 规范（对局内）
- 每辆坦克：头顶三角标（敌红/友青）、血条弧形细线 + HP 数值；受伤闪红边框。
- 自瞄辅助开启时：目标框 + 提前量准星线（虚线 + 命中点菱形）。
- 事件提示（我被命中/发现目标）以全息小字滑动进入事件流，不出弹窗打断。
- 音效：低音轰鸣引擎、激光/实弹射击、命中"咔"、爆炸低频 + 短噪声；UI 操作 UI click 高频短促音。
- 可选"战术指挥层"：显示各 bot 意图轨迹（决策轨迹），这是复盘模式特有卖点。

---

## 10. 数据模型草案（简）

- `users`：id / 昵称 / 邮箱 / 天梯分 / 段位 / 信用分
- `strategy_packs`：id / user_id / name / version / manifest / build / brain_code / model_ref / status(draft|testing|published|banned) / 冒烟测试报告
- `tanks`(instance)：对局实例中的坦克：strategy_pack_id + 实时状态(HP/pos/cooldown...)
- `teams` / `team_members`：车队与挂载槽
- `matches`：id / mode / map_id / seed / rules_snapshot / participants / result / replay_uri / server_version(规则冻结版本)
- `match_events` / `replay_chunks`：回放流
- `leaderboard`、`seasons`、`audit_logs`

---

## 11. 里程碑规划（建议）

| 里程碑 | 范围 | 出口标准 |
|---|---|---|
| **M1 战斗内核 + 1v1** | 确定性引擎、地图/墙体/弹道/命中/HP 结算、JS 沙箱执行、最小策略契约、浏览器渲染、A vs B 单挑流程、简易上传 | 两台内置策略可稳定自动对打 100 局，回放一致 |
| **M2 策略包与平台闭环** | 编辑器（属性加点 + 代码 + 冒烟测试 + 版本）、试练台、账户、战绩、基础大厅 UI（高科技视觉 v1） | 外部用户可上传并完成首次对战 |
| **M3 团战 + 观战回放** | 2v2~5v5 组队、队友通信、观战多视角、回放时间轴与 Debug 层 | 团战全流程可用 |
| **M4 天梯与生态** | 匹配、ELO、赛季、排行榜、房间系统完整版、自定义规则 | 稳定支撑周赛 |
| **M5 模型策略** | ONNX 推理运行时、训练模板仓库(Python/环境)、模型 vs 规则对局、特征可视化 | 强化学习模型可打败官方示范规则 Bot |
| **M6 打磨运营** | 音效动效打磨、动效观感升级、赛事/锦标赛、反作弊强化、国际化 | 商业化/活动可跑通 |

---

## 12. 验收标准（V1 MVP 核心场景）

1. 用户 A 上传策略包 A、用户 B 上传策略包 B 后，可建立一局 1v1：**双方全自动交战，任一方 HP 归零判负，另一方胜**（用户核心诉求 #1、#2）。
2. 两个策略包分别只调高"伤害"或只调高"血量"时，对局结果/用时呈现可解释差异（属性维度生效，#4）。
3. 两个**同样的属性 build**，分别挂不同的瞄准/走位规则策略，可稳定复现不同的胜率（决策维度生效，#4）。
4. 选择策略包"移动速度/血量/攻击速度/基础伤害/瞄准策略"组合变化时，结算报告展示相应参数量化差异（#4 覆盖）。
5. 团战模式：每队 3 辆，含至少两队、每队多人挂载，可完成一局并正确结算（#3）。
6. 上传恶意代码（如 `while(true){}`、试图 `fetch`）被沙箱拦截，给出可读错误且不影响其它对局（#2 安全面）。
7. 回放同一种子得到逐 tick 一致结果（NFR 确定性）。
8. 全站核心界面符合 §9 视觉规范：深空底、霓虹能量色、全息 HUD 元素齐全（#6）。

---

## 13. 风险与开放问题

| 风险/问题 | 说明 | 建议 |
|---|---|---|
| 沙箱安全与确定性冲突 | 强隔离 + 确定性执行较难两全 | 先用"受限 Worker + 确定性 PRNG + 时间配额"，评审后确定 QuickJS-WASM 路线 |
| 确定性引擎跨端漂移 | 浮点在不同端不一致破坏回放 | 引擎内统一定点/整数运算或规范 IEEE 舍入 |
| 规则写得太烂导致"最强大脑是官方 bot" | 新手挫败 | 内置分层 Bot 家族(呆瓜→老兵→大神)引导升级 |
| 数值平衡风险 | 某些 build 无解 | CP 权重可热调（赛季间），记录全量对局数据离线分析 |
| 浏览器沙箱语义差异 | 不同浏览器 JS 差异 | 只保证最新两大版本，CI 跑多浏览器冒烟 |
| 模型策略门槛 | 普通用户无法训练 | 官方提供"行为树/可视化规则"作为中间形态，模型作为进阶玩法 |

---

## 附录 A：参考来源

- Robocode Tank Royale — github.com/robocode-dev/tank-royale，robocode.dev（事件驱动 API、WebSocket、无头批量比赛、观战器）
- CyberTank 3v3 AI Tank Battle Arena — cybertank.squidcode.com（浏览器 DSL、传感器/燃料/弹药资源、3v3 槽位）
- AgenTank — agentank.ai，开源示例 github.com/imsancn/AgenTank_King（JS `onIdle(me,enemy,game)` 回调、技能帧/冷却、段位制）
- 百度之星《坦克大战 AI》（策略+搜索算法的对抗竞赛形式）
